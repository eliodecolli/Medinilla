syntax = "proto3";

package medinilla.core.SharedContracts.comms;

import "ocpp/OcppMessage.proto";

enum CommsFlag {
	Set = 0;
	Remove = 1;
}

message CommunicationChannelSignal {
	CommsFlag Flag = 1;
	medinilla.core.SharedContracts.comms.ocpp.OcppMessage Message = 2;
}