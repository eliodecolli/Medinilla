syntax = "proto3";

package medinilla.core.SharedContracts.comms;

enum ChannelType {
	OcppEvent = 0;
	Other = 1;
}

enum CommsFlag {
	Set = 0;
	Remove = 1;
}

message CommunicationChannelSignal {
	string ChannelName = 1;
	ChannelType ChannelType = 2;
	optional CommsFlag Flag = 3;
}